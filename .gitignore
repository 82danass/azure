# mov writes these. Private keys, initial passwords and deployment state are
# never public; the configuration that produces them is.
mov-workspace/keys/
mov-workspace/state/

# mov docs transcripts carry the initial passwords from `az ad user create`.
v*/commands*.md

# mov audit reads the billing account: profile ids, address, card, balance.
v*/audit.md
